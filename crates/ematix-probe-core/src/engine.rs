//! Engine modules. `engine::data` houses the data-probe types;
//! `engine::load` lands in Phase 4 with the constant-rate scheduler
//! and VU pool for HTTP/SQL load probes.

pub mod data;
pub mod scan;
